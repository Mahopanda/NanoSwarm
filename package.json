{
  "name": "nanoswarm",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --watch packages/server/src/index.ts",
    "test": "bun test",
    "lint": "eslint packages/*/src",
    "format": "prettier --write ."
  }
}
